export interface CardTheme {
  bg: string;
  bgGrad: string;
  title: string;
  desc: string;
  num: string;
  rule: string;
}

export interface CredentialItem {
  title: string;
  subtitle?: string;
}
